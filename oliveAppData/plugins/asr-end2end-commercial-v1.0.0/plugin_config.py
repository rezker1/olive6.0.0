# MODE

user_config = dict(
    # USER specifed parameters
    sad_threshold        = 0.0,
    # If unicode normalization is not wanted, omit or set to 'None'.
    unicode_normalization = 'None',
    # use LM or not for the ctcdecoding
    LM_process = True,
)
