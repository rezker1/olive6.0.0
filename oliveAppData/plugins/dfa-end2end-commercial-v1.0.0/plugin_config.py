user_config = dict(
    # USER specified parameters
    min_speech = 0.3,
    # SAD
    sad_threshold = 0.5,
    # Detection options
    threshold = 0.0,
    # DF score offset
    score_offset = 0.0,
)
