user_config = dict(
# DETECTION OPTIONS
threshold  = 0.0,
face_detection_threshold = 0.95,

# PROCESSING OPTIONS
keep_only_one_score_per_enrollee = True,
keep_only_highest_score_per_face = True,
keep_face_with_highest_score_if_overlapping = True,
)
